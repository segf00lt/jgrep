#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

enum OPCODES {
	CHAR = 1,
	JUMP,
	WILD,
	ALT,
	QUEST,
	PLUS,
	STAR,
	BACK,
	CLASS,
	MATCH,
	BLINE,
	ELINE,
};

int main(int argc, char* argv[]) {
	return 0;
}
